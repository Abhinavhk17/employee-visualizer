/**
 * Application Routes Configuration
 * 
 * Currently empty as this is a single-page application that displays
 * employee time data visualization on the main route.
 * 
 * Could be expanded in the future to include:
 * - Employee detail pages
 * - Settings/configuration pages
 * - Different chart view modes
 * - Historical data views
 */

import { Routes } from '@angular/router';

export const routes: Routes = [
  // Currently no routes defined - single page application
  // Future routes could include:
  // { path: 'employee/:id', component: EmployeeDetailComponent },
  // { path: 'settings', component: SettingsComponent },
  // { path: '', redirectTo: '/dashboard', pathMatch: 'full' }
];
